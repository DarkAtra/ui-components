{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna clean --yes && lerna bootstrap",
    "build": "lerna run build --stream",
    "start": "lerna run --scope demo start --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
